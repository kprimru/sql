USE [DBF_NAH]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
ALTER FUNCTION [dbo].[MoneyMin]
(
	@A	MONEY,
	@B	MONEY
)
RETURNS MONEY
AS
BEGIN
	DECLARE @RES MONEY

	IF @A > @B
		SET @RES = @B
	ELSE
		SET @RES = @A

	RETURN @RES
END

GO
