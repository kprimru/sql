USE [DBF]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE FUNCTION [dbo].[MoneyMax]
(
	@A	MONEY,
	@B	MONEY	
)
RETURNS MONEY
AS
BEGIN
	DECLARE @RES MONEY

	IF @A > @B
		SET @RES = @A
	ELSE
		SET @RES = @B

	RETURN @RES
END
