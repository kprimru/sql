USE [DBF]
	GO
	SET ANSI_NULLS ON
	GO
	SET QUOTED_IDENTIFIER ON
	GO
	CREATE PROCEDURE [Price].[SYSTEM_TYPE_SELECT]
AS
BEGIN
	SET NOCOUNT ON;

	SELECT SST_ID, SST_CAPTION
	FROM dbo.SystemTypeTable
	ORDER BY SST_ORDER
END
