USE [DBF]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[ACT_1C_DATE_SELECT]
AS
BEGIN
	SET NOCOUNT ON;

	SELECT DISTINCT DATE
	FROM dbo.Act1C
	WHERE TOTAL = 1
	ORDER BY DATE DESC
END
