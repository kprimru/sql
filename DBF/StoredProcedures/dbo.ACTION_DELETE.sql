USE [DBF]
	GO
	SET ANSI_NULLS ON
	GO
	SET QUOTED_IDENTIFIER ON
	GO
	CREATE PROCEDURE [dbo].[ACTION_DELETE]
	@ID	SMALLINT
AS
BEGIN
	SET NOCOUNT ON;

	DELETE
	FROM dbo.ActionPeriod
	WHERE AP_ID_AC = @ID

	DELETE 
	FROM dbo.Action
	WHERE ACTN_ID = @ID
END
