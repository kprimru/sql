USE [DBF]
	GO
	SET ANSI_NULLS ON
	GO
	SET QUOTED_IDENTIFIER ON
	GO
	CREATE PROCEDURE [dbo].[INCOME_DEFAULT_GET]
AS
BEGIN
	SET NOCOUNT ON;

	SELECT IT_ID, IT_NAME
	FROM dbo.IncomeTypeTable
	WHERE IT_ID = 1
END
