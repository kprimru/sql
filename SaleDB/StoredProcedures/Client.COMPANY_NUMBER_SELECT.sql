USE [SaleDB]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [Client].[COMPANY_NUMBER_SELECT]
	@ID	UNIQUEIDENTIFIER
AS
BEGIN
	SELECT NUMBER
	FROM Client.Company
	WHERE ID = @ID
END
