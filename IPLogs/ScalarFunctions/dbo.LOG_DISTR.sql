USE [IPLogs]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
ALTER FUNCTION [dbo].[LOG_DISTR]
(
	@NAME	NVARCHAR(512)
)
RETURNS INT
AS
BEGIN
	DECLARE @RESULT	INT

	SET @RESULT = dbo.LOG_PARSE(@NAME, 'DISTR')

	RETURN @RESULT
END
