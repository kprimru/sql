USE [DocumentClaim]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
ALTER FUNCTION [Maintenance].[G_MaintenanceMode]
()
RETURNS BIT
AS
BEGIN
	DECLARE @RES BIT

	SELECT @RES = MAINTENANCE_MODE
	FROM Maintenance.Settings
	WHERE STATUS = 1

	RETURN @RES
END
GO
