USE [FirstInstall]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [Distr].[NET_ALL_SELECT]
	@RC INT = NULL OUTPUT
AS
BEGIN
	SET NOCOUNT ON

	SELECT	*
	FROM	Distr.NetAll
	ORDER BY TECH, COEF, NET_COUNT
	
	SELECT	@RC = @@ROWCOUNT
END
