USE [FirstInstall]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
IF OBJECT_ID('[Clients].[CLIENTS_DELETED]', 'P ') IS NULL EXEC('CREATE PROCEDURE [Clients].[CLIENTS_DELETED]  AS SELECT 1')
GO
ALTER PROCEDURE [Clients].[CLIENTS_DELETED]
	@RC INT = NULL OUTPUT
AS
BEGIN
	SET NOCOUNT ON

	SELECT	*
	FROM	[Clients].[ClientDeleted]

	SELECT	@RC = @@ROWCOUNT
END
GO
GRANT EXECUTE ON [Clients].[CLIENTS_DELETED] TO rl_clients_r;
GO
