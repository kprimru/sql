USE [FirstInstall]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
IF OBJECT_ID('[Book].[BOOK_PRICE_LAST]', 'P ') IS NULL EXEC('CREATE PROCEDURE [Book].[BOOK_PRICE_LAST]  AS SELECT 1')
GO
ALTER PROCEDURE [Book].[BOOK_PRICE_LAST]
	@DT	DATETIME = NULL OUTPUT
AS
BEGIN
	SET NOCOUNT ON;

	SELECT	@DT	=	MAX(BPMS_LAST)
	FROM	Book.BookPrice
END
GO
GRANT EXECUTE ON [Book].[BOOK_PRICE_LAST] TO rl_book_price_r;
GO
