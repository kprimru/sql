USE [FirstInstall]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
IF OBJECT_ID('[Distr].[HOST_LAST]', 'P ') IS NULL EXEC('CREATE PROCEDURE [Distr].[HOST_LAST]  AS SELECT 1')
GO
ALTER PROCEDURE [Distr].[HOST_LAST]
	@DT	DATETIME = NULL OUTPUT
AS
BEGIN
	SET NOCOUNT ON;

	SELECT	@DT = MAX(HSTMS_LAST)
	FROM	Distr.Hosts
END
GO
GRANT EXECUTE ON [Distr].[HOST_LAST] TO rl_host_r;
GO
