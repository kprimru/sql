USE [FirstInstall]
	GO
	SET ANSI_NULLS ON
	GO
	SET QUOTED_IDENTIFIER ON
	GO
	CREATE PROCEDURE [Clients].[CLIENTS_SELECT]
	@RC INT = NULL OUTPUT
AS
BEGIN
	SET NOCOUNT ON	

	SELECT	*
	FROM	[Clients].[ClientActive]	

	SELECT	@RC = @@ROWCOUNT
END
