USE [FirstInstall]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [Install].[INSTALL_PERSONAL_SELECT]
AS
BEGIN
	SET NOCOUNT ON;

	SELECT *
	FROM Personal.PersonalLast
	ORDER BY PER_NAME
END
