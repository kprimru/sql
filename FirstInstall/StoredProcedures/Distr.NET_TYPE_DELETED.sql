USE [FirstInstall]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
IF OBJECT_ID('[Distr].[NET_TYPE_DELETED]', 'P ') IS NULL EXEC('CREATE PROCEDURE [Distr].[NET_TYPE_DELETED]  AS SELECT 1')
GO
ALTER PROCEDURE [Distr].[NET_TYPE_DELETED]
	@RC INT = NULL OUTPUT
AS
BEGIN
	SET NOCOUNT ON

	SELECT	*
	FROM	[Distr].[NetTypeDeleted]

	SELECT	@RC = @@ROWCOUNT
END
GO
GRANT EXECUTE ON [Distr].[NET_TYPE_DELETED] TO rl_net_type_r;
GO
