USE [FirstInstall]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
IF OBJECT_ID('[Book].[BOOK_BONUS_LAST]', 'P ') IS NULL EXEC('CREATE PROCEDURE [Book].[BOOK_BONUS_LAST]  AS SELECT 1')
GO
ALTER PROCEDURE [Book].[BOOK_BONUS_LAST]
	@DT	DATETIME = NULL OUTPUT
AS
BEGIN
	SET NOCOUNT ON;

	SELECT	@DT	=	MAX(BBMS_LAST)
	FROM	Book.BookBonus
END
GO
GRANT EXECUTE ON [Book].[BOOK_BONUS_LAST] TO rl_book_bonus_r;
GO
