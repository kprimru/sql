USE [ClientDB]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [USR].[OS_FAMILY_GET]
	@ID		INT
AS
BEGIN
	SET NOCOUNT ON;

	SELECT OF_NAME
	FROM USR.OSFamily
	WHERE OF_ID = @ID
END