USE [ClientDB]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[LETTER_GET]
	@id INT
AS
BEGIN
	SET NOCOUNT ON;

	SELECT LetterData
	FROM dbo.LetterTable
	WHERE LetterID = @id
END