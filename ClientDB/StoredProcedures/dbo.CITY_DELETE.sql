USE [ClientDB]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[CITY_DELETE]
	@ID	UNIQUEIDENTIFIER
AS
BEGIN
	SET NOCOUNT ON;

	DELETE 
	FROM dbo.Street
	WHERE ST_ID_CITY = @ID

	DELETE
	FROM dbo.City
	WHERE CT_ID = @ID
END