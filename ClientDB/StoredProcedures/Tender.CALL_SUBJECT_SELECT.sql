USE [ClientDB]
	GO
	SET ANSI_NULLS ON
	GO
	SET QUOTED_IDENTIFIER ON
	GO
	CREATE PROCEDURE [Tender].[CALL_SUBJECT_SELECT]
AS
BEGIN
	SET NOCOUNT ON;

	SELECT DISTINCT SUBJECT
	FROM Tender.Call
	WHERE STATUS = 1
	ORDER BY SUBJECT
END
