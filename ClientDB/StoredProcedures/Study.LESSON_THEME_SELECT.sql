USE [ClientDB]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [Study].[LESSON_THEME_SELECT]
AS
BEGIN
	SET NOCOUNT ON;

	SELECT DISTINCT THEME
	FROM Study.Lesson
	WHERE STATUS = 1
	ORDER BY THEME
END
