USE [ClientDB]
	GO
	SET ANSI_NULLS ON
	GO
	SET QUOTED_IDENTIFIER ON
	GO
	CREATE PROCEDURE [dbo].[HOSTS_GET]
	@ID	INT
AS
BEGIN
	SET NOCOUNT ON;

	SELECT HostShort, HostReg, HostOrder
	FROM dbo.Hosts
	WHERE HostID = @ID
END