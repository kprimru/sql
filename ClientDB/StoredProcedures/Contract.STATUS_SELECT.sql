USE [ClientDB]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [Contract].[STATUS_SELECT]
AS
BEGIN
	SET NOCOUNT ON;
	
	SELECT ID, NAME, IND
	FROM Contract.Status
	ORDER BY NAME
END
