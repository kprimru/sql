USE [ClientDB]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[RIVAL_STATUS_GET]
	@ID	INT
AS
BEGIN
	SET NOCOUNT ON;

	SELECT RS_NAME
	FROM dbo.RivalStatus
	WHERE RS_ID = @ID
END