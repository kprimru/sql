USE [ClientDB]
	GO
	SET ANSI_NULLS ON
	GO
	SET QUOTED_IDENTIFIER ON
	GO
	CREATE PROCEDURE [USR].[PROCESSOR_FAMILY_GET]
	@ID		INT
AS
BEGIN
	SET NOCOUNT ON;

	SELECT PF_NAME
	FROM USR.ProcessorFamily
	WHERE PF_ID = @ID
END