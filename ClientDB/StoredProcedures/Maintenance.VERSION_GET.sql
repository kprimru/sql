USE [ClientDB]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [Maintenance].[VERSION_GET]
AS
BEGIN
	SET NOCOUNT ON;

	SELECT VERSION, DATE, INSTALL
	FROM Maintenance.Version
END
