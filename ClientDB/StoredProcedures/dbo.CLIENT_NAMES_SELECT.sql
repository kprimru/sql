USE [ClientDB]
GO
SET ANSI_NULLS ON
GO
SET QUOTED_IDENTIFIER ON
GO
CREATE PROCEDURE [dbo].[CLIENT_NAMES_SELECT]
	@ID	INT
AS
BEGIN
	SET NOCOUNT ON;

	SELECT NAME
	FROM dbo.ClientNames
	WHERE ID_CLIENT = @ID
	ORDER BY NAME
END